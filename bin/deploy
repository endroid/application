#!/bin/bash

set -ex

rm -rf application
git clone git@github.com:endroid/application.git

(
    cd application
    docker-compose stop
    docker-compose up -d
    bin/build-and-run
    echo APP_ENV=prod > symfony/application/.env.local
    docker-compose exec nginx /etc/nginx/ssl/certbot
)
