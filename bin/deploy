#!/bin/bash

set -ex

rm -rf application
git clone git@github.com:endroid/application.git

(
    cd application
    docker-compose stop
    docker system prune -a --volumes
    docker-compose up -d
    bin/build-and-run
    cp ../.env.local symfony/application/
    docker-compose exec nginx /etc/nginx/ssl/certbot
)
